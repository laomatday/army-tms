
<!-- 
  HƯỚNG DẪN CẤU HÌNH ANDROID MANIFEST
  File đích: android/app/src/main/AndroidManifest.xml
-->

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="com.armytms.v2026">

    <uses-permission android:name="android.permission.INTERNET" />
    
    <!-- Các quyền cần thiết cho Camera và Định vị (khớp với Web App) -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme">

      <!-- 
        QUAN TRỌNG: Cấu hình windowSoftInputMode="adjustPan" 
        
        Mặc định React Native thường dùng "adjustResize". 
        Tuy nhiên, với WebView toàn màn hình có các thành phần điều hướng (BottomNav) cố định:
        
        - "adjustResize": Sẽ đẩy toàn bộ View lên, làm biến dạng giao diện Web App hoặc đẩy BottomNav lên giữa màn hình.
        - "adjustPan": Sẽ giữ nguyên View, chỉ trượt nội dung lên để input không bị che khuất. 
          Điều này giúp loại bỏ cảm giác "nhảy" giao diện và ẩn các thanh điều hướng không mong muốn.
      -->
      
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustPan" 
        android:exported="true">
        
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>
    </application>
</manifest>
